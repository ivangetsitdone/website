fastapi==0.141.1
# Pinned explicitly, not left to FastAPI's floor of >=0.46.0: starlette 0.48.0 shipped an
# unauthenticated DoS in FileResponse's Range parsing, which every /static, /media and
# /print request goes through. Fixed in 0.49.1; 1.3.1 also closes the request.url
# reconstruction issues that the cache middleware's path check relies on.
starlette==1.6.0
uvicorn==0.53.0
jinja2==3.1.6
